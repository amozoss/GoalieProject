package com.willoughby.goalie.db.generated;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table REMINDER.
*/
public class ReminderDao extends AbstractDao<Reminder, Long> {

    public static final String TABLENAME = "REMINDER";

    /**
     * Properties of entity Reminder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property FireDate = new Property(1, java.util.Date.class, "fireDate", false, "FIRE_DATE");
        public final static Property FireTime = new Property(2, java.util.Date.class, "fireTime", false, "FIRE_TIME");
        public final static Property IsSpecificDate = new Property(3, Boolean.class, "isSpecificDate", false, "IS_SPECIFIC_DATE");
        public final static Property RepeatsOnDayOfTheWeek = new Property(4, Integer.class, "repeatsOnDayOfTheWeek", false, "REPEATS_ON_DAY_OF_THE_WEEK");
        public final static Property HabitualGoalId = new Property(5, long.class, "habitualGoalId", false, "HABITUAL_GOAL_ID");
    };

    private DaoSession daoSession;

    private Query<Reminder> habitualGoal_RemindersQuery;

    public ReminderDao(DaoConfig config) {
        super(config);
    }
    
    public ReminderDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'REMINDER' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'FIRE_DATE' INTEGER," + // 1: fireDate
                "'FIRE_TIME' INTEGER," + // 2: fireTime
                "'IS_SPECIFIC_DATE' INTEGER," + // 3: isSpecificDate
                "'REPEATS_ON_DAY_OF_THE_WEEK' INTEGER," + // 4: repeatsOnDayOfTheWeek
                "'HABITUAL_GOAL_ID' INTEGER NOT NULL );"); // 5: habitualGoalId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'REMINDER'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Reminder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        java.util.Date fireDate = entity.getFireDate();
        if (fireDate != null) {
            stmt.bindLong(2, fireDate.getTime());
        }
 
        java.util.Date fireTime = entity.getFireTime();
        if (fireTime != null) {
            stmt.bindLong(3, fireTime.getTime());
        }
 
        Boolean isSpecificDate = entity.getIsSpecificDate();
        if (isSpecificDate != null) {
            stmt.bindLong(4, isSpecificDate ? 1l: 0l);
        }
 
        Integer repeatsOnDayOfTheWeek = entity.getRepeatsOnDayOfTheWeek();
        if (repeatsOnDayOfTheWeek != null) {
            stmt.bindLong(5, repeatsOnDayOfTheWeek);
        }
        stmt.bindLong(6, entity.getHabitualGoalId());
    }

    @Override
    protected void attachEntity(Reminder entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Reminder readEntity(Cursor cursor, int offset) {
        Reminder entity = new Reminder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : new java.util.Date(cursor.getLong(offset + 1)), // fireDate
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // fireTime
            cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0, // isSpecificDate
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // repeatsOnDayOfTheWeek
            cursor.getLong(offset + 5) // habitualGoalId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Reminder entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFireDate(cursor.isNull(offset + 1) ? null : new java.util.Date(cursor.getLong(offset + 1)));
        entity.setFireTime(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setIsSpecificDate(cursor.isNull(offset + 3) ? null : cursor.getShort(offset + 3) != 0);
        entity.setRepeatsOnDayOfTheWeek(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setHabitualGoalId(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Reminder entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Reminder entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "reminders" to-many relationship of HabitualGoal. */
    public List<Reminder> _queryHabitualGoal_Reminders(long habitualGoalId) {
        synchronized (this) {
            if (habitualGoal_RemindersQuery == null) {
                QueryBuilder<Reminder> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.HabitualGoalId.eq(null));
                queryBuilder.orderRaw("FIRE_DATE ASC");
                habitualGoal_RemindersQuery = queryBuilder.build();
            }
        }
        Query<Reminder> query = habitualGoal_RemindersQuery.forCurrentThread();
        query.setParameter(0, habitualGoalId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getHabitualGoalDao().getAllColumns());
            builder.append(" FROM REMINDER T");
            builder.append(" LEFT JOIN HABITUAL_GOAL T0 ON T.'HABITUAL_GOAL_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Reminder loadCurrentDeep(Cursor cursor, boolean lock) {
        Reminder entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        HabitualGoal habitualGoal = loadCurrentOther(daoSession.getHabitualGoalDao(), cursor, offset);
         if(habitualGoal != null) {
            entity.setHabitualGoal(habitualGoal);
        }

        return entity;    
    }

    public Reminder loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Reminder> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Reminder> list = new ArrayList<Reminder>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Reminder> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Reminder> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
