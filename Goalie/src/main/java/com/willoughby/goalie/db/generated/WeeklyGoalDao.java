package com.willoughby.goalie.db.generated;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table WEEKLY_GOAL.
*/
public class WeeklyGoalDao extends AbstractDao<WeeklyGoal, Long> {

    public static final String TABLENAME = "WEEKLY_GOAL";

    /**
     * Properties of entity WeeklyGoal.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Completed = new Property(1, Integer.class, "completed", false, "COMPLETED");
        public final static Property Goal = new Property(2, Integer.class, "goal", false, "GOAL");
        public final static Property WeekEnd = new Property(3, java.util.Date.class, "weekEnd", false, "WEEK_END");
        public final static Property WeekStart = new Property(4, java.util.Date.class, "weekStart", false, "WEEK_START");
        public final static Property HabitualGoalId = new Property(5, long.class, "habitualGoalId", false, "HABITUAL_GOAL_ID");
    };

    private DaoSession daoSession;

    private Query<WeeklyGoal> habitualGoal_WeeklyGoalsQuery;

    public WeeklyGoalDao(DaoConfig config) {
        super(config);
    }
    
    public WeeklyGoalDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'WEEKLY_GOAL' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'COMPLETED' INTEGER," + // 1: completed
                "'GOAL' INTEGER," + // 2: goal
                "'WEEK_END' INTEGER," + // 3: weekEnd
                "'WEEK_START' INTEGER," + // 4: weekStart
                "'HABITUAL_GOAL_ID' INTEGER NOT NULL );"); // 5: habitualGoalId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'WEEKLY_GOAL'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, WeeklyGoal entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer completed = entity.getCompleted();
        if (completed != null) {
            stmt.bindLong(2, completed);
        }
 
        Integer goal = entity.getGoal();
        if (goal != null) {
            stmt.bindLong(3, goal);
        }
 
        java.util.Date weekEnd = entity.getWeekEnd();
        if (weekEnd != null) {
            stmt.bindLong(4, weekEnd.getTime());
        }
 
        java.util.Date weekStart = entity.getWeekStart();
        if (weekStart != null) {
            stmt.bindLong(5, weekStart.getTime());
        }
        stmt.bindLong(6, entity.getHabitualGoalId());
    }

    @Override
    protected void attachEntity(WeeklyGoal entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public WeeklyGoal readEntity(Cursor cursor, int offset) {
        WeeklyGoal entity = new WeeklyGoal( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // completed
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // goal
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // weekEnd
            cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)), // weekStart
            cursor.getLong(offset + 5) // habitualGoalId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, WeeklyGoal entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCompleted(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setGoal(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setWeekEnd(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setWeekStart(cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)));
        entity.setHabitualGoalId(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(WeeklyGoal entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(WeeklyGoal entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "weeklyGoals" to-many relationship of HabitualGoal. */
    public List<WeeklyGoal> _queryHabitualGoal_WeeklyGoals(long habitualGoalId) {
        synchronized (this) {
            if (habitualGoal_WeeklyGoalsQuery == null) {
                QueryBuilder<WeeklyGoal> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.HabitualGoalId.eq(null));
                habitualGoal_WeeklyGoalsQuery = queryBuilder.build();
            }
        }
        Query<WeeklyGoal> query = habitualGoal_WeeklyGoalsQuery.forCurrentThread();
        query.setParameter(0, habitualGoalId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getHabitualGoalDao().getAllColumns());
            builder.append(" FROM WEEKLY_GOAL T");
            builder.append(" LEFT JOIN HABITUAL_GOAL T0 ON T.'HABITUAL_GOAL_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected WeeklyGoal loadCurrentDeep(Cursor cursor, boolean lock) {
        WeeklyGoal entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        HabitualGoal habitualGoal = loadCurrentOther(daoSession.getHabitualGoalDao(), cursor, offset);
         if(habitualGoal != null) {
            entity.setHabitualGoal(habitualGoal);
        }

        return entity;    
    }

    public WeeklyGoal loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<WeeklyGoal> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<WeeklyGoal> list = new ArrayList<WeeklyGoal>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<WeeklyGoal> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<WeeklyGoal> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
